## TODO

- 初期姿勢チェック

- 学習データのRGB順チェック


## docker環境構築

```bash
cd /home/robo/work/tnakagawa/act
docker compose build
docker compose up -d
```


## コンテナにアクセス

```bash
# コンテナ名を指定して起動
docker exec it act-py312 bash

# もしくはdocker composeから起動
cd /home/robo/work/tnakagawa/act
docker compose exec latest bash
```


## プレイバック

```bash
(venv) root@act-py312:/workspace/act/act# PYTHONPATH=$PYTHONPATH:. python src/robot_controller/deploy.py --ckpt_path log/20260606_1518_20/policy_best.ckpt --dataset_dir /workspace/datasets/npz_org/ --playback --freq 30 --device -1
```

## オンラインテスト

```bash
# 首の関節角度: [0, 0.7]
rostopic pub /maharo/head/online_joint_states sensor_msgs/JointState "header:
  seq: 0
  stamp: {secs: 0, nsecs: 0}
  frame_id: ''
name: ['']
position: [0,0.7]
velocity: [0]
effort: [0]" 

# 推論
PYTHONPATH=$PYTHONPATH:. python src/robot_controller/deploy.py --ckpt_path log/20260606_1518_20/policy_best.ckpt --dataset_dir /workspace/datasets/npz_org/ --freq 30 --device 0
```